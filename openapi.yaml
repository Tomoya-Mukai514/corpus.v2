openapi: 3.1.0
info:
  title: J-Stage Search Proxy
  version: 1.0.0
servers:
  - url: https://corpus-v2-0i2i.onrender.com
paths:
  /answer:
    get:
      operationId: answerJstage
      summary: Get candidate papers for a query
      parameters:
        - in: query
          name: q
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Candidate results
